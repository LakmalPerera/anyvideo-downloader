{
  "name": "anyvideo-downloader",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
   "postinstall": "python -m pip install yt-dlp || pip install yt-dlp || true"

  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
